name: Deploy dekorsever

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Connect to server
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.REGRU_HOST }}
          username: ${{ secrets.REGRU_USER }}
          key: ${{ secrets.REGRU_KEY }}
          script: |
            cd /var/www/u1538455/data/www/dekorsever.ru/wp-content/themes/dekorsever-wp
            git pull
            
            #Если кто то изменил файлы на сервере минуя GitHub, перезаписываем версию на сервере из GitHub
            #cd /var/www/u1538455/data/www/dekorsever.ru/wp-content/themes/dekorsever-wp
            #echo "Сбрасываем локальные изменения..."
            #git reset --hard HEAD
            #echo "Получаем обновления из репозитория..."
            #git pull origin main
            #echo "Обновление завершено!"